Prefer to code to a file before executing.
When running simulations, use all cores.
Optimize simulations by using monte-carlo to compute a PMF.
Make sure to sanity check the code that uses the PMF against a simple monte-carlo.